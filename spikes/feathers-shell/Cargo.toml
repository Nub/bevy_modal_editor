[package]
name = "spike_feathers_shell"
description = "M0 spike 3: can bevy_ui + feathers carry the editor shell?"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
bevy = { workspace = true, features = ["bevy_feathers"] }

[lints]
workspace = true
